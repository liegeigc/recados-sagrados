<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<!-- Google tag (gtag.js) - GA4 -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-9M4WP0P9Q9"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-9M4WP0P9Q9');
</script>
<title>Obrigada — O Código do Amor Divino</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cinzel:ital,wght@0,400;0,500;1,400;1,500&family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,400;1,500&display=swap" rel="stylesheet">
<style>
  *{box-sizing:border-box;margin:0;padding:0}
  :root{
    --bg-deep:#110827;
    --gold:#c9a24b;
    --gold-bright:#e3bb6b;
    --cream:#e8e5d8;
    --cream-dim:#b8b3a3;
    --lavender:#a89bbf;
    --lavender-bright:#c5b8d6;
    --line:rgba(168,155,191,0.2);
  }

  html,body{
    background:var(--bg-deep);
    color:var(--cream);
    font-family:"Cormorant Garamond",serif;
    font-size:16px;line-height:1.5;
    min-height:100vh;
    -webkit-font-smoothing:antialiased;
  }

  .page{
    max-width:680px;margin:0 auto;
    padding:24px 28px 28px;
    text-align:center;
    background:
      radial-gradient(ellipse at top, rgba(201,162,75,0.1), transparent 60%),
      radial-gradient(ellipse at bottom, rgba(201,162,75,0.04), transparent 50%),
      var(--bg-deep);
    min-height:100vh;
    position:relative;
    display:flex;flex-direction:column;justify-content:center;
  }

  /* Starfield sutil */
  .star{
    position:absolute;width:2px;height:2px;
    background:rgba(232,229,216,0.4);border-radius:50%;
    pointer-events:none;
  }

  .selo{
    width:64px;margin:0 auto 12px;
    filter:drop-shadow(0 0 28px rgba(201,162,75,0.5));
    animation:pulse 3s ease-in-out infinite;
  }
  .selo img{width:100%;height:auto;display:block}

  @keyframes pulse{
    0%,100%{transform:scale(1)}
    50%{transform:scale(1.04)}
  }

  .eyebrow{
    font-family:"Cinzel",serif;
    font-size:10px;letter-spacing:4px;text-transform:uppercase;
    color:var(--gold);font-weight:500;
    margin-bottom:8px;
  }

  h1{
    font-family:"Cinzel",serif;
    font-style:italic;font-weight:500;
    font-size:28px;line-height:1.2;
    color:var(--gold);letter-spacing:.4px;
    margin:0 auto 8px;
    max-width:560px;
  }

  .subtitle{
    font-style:italic;font-size:16px;
    color:var(--lavender-bright);
    margin:0 auto 14px;
    max-width:480px;line-height:1.4;
  }

  .divider{
    width:46px;height:1px;background:var(--gold);
    margin:0 auto 14px;opacity:.6;
  }

  .message{
    max-width:560px;margin:0 auto 16px;
    font-size:14.5px;color:var(--cream-dim);line-height:1.55;
    text-align:center;
  }
  .message p{margin-bottom:6px}
  .message p:last-child{margin-bottom:0}
  .message strong{color:var(--cream);font-weight:500}
  .message em{color:var(--lavender-bright);font-style:italic}

  /* Box de próximos passos — compacto */
  .next-box{
    background:rgba(201,162,75,0.04);
    border:1px solid var(--line);
    padding:14px 22px;
    margin:0 auto 16px;
    max-width:520px;
    text-align:left;
    border-radius:4px;
  }
  .next-title{
    font-family:"Cinzel",serif;
    font-size:10px;letter-spacing:2.5px;text-transform:uppercase;
    color:var(--gold);font-weight:500;
    margin-bottom:8px;text-align:center;
  }
  .next-item{
    display:flex;gap:10px;align-items:flex-start;
    margin-bottom:5px;font-size:13.5px;
    color:var(--cream-dim);line-height:1.5;
  }
  .next-item:last-child{margin-bottom:0}
  .next-icon{
    color:var(--gold);font-size:14px;flex-shrink:0;
    line-height:1.3;
  }
  .next-item strong{color:var(--cream);font-weight:500}

  /* BOTÃO */
  .cta-wrap{
    margin:0 auto 14px;
    text-align:center;
  }
  .cta-btn{
    display:inline-block;
    font-family:"Cinzel",serif;
    font-size:12.5px;letter-spacing:2.5px;text-transform:uppercase;font-weight:500;
    background:var(--gold);color:var(--bg-deep);
    padding:14px 36px;text-decoration:none;
    border-radius:2px;
    box-shadow:
      0 0 0 1px var(--gold-bright),
      0 0 32px rgba(201,162,75,0.4);
    transition:all .2s;
  }
  .cta-btn:hover{background:var(--gold-bright);transform:translateY(-1px)}

  .cta-sub{
    margin-top:6px;
    font-size:12.5px;color:var(--cream-dim);font-style:italic;
  }

  /* Suporte */
  .support{
    margin-top:14px;
    padding-top:12px;
    border-top:1px solid var(--line);
    text-align:center;
  }
  .support-label{
    font-family:"Cinzel",serif;
    font-size:9.5px;letter-spacing:2.5px;text-transform:uppercase;
    color:var(--lavender);margin-bottom:4px;
  }
  .support-email{
    font-family:"Cormorant Garamond",serif;
    font-style:italic;font-size:14.5px;
    color:var(--gold);
  }
  .support-email a{color:var(--gold);text-decoration:none}

  .blessing{
    margin-top:12px;
    font-family:"Cormorant Garamond",serif;
    font-style:italic;font-size:14px;
    color:var(--lavender-bright);
    max-width:480px;margin-left:auto;margin-right:auto;
    line-height:1.45;
  }

  .signature{
    margin-top:8px;
    font-family:"Cinzel",serif;
    font-size:10px;letter-spacing:3.5px;text-transform:uppercase;
    color:var(--gold);
  }

  @media (max-width:680px){
    .page{padding:20px 18px;min-height:auto}
    h1{font-size:24px}
    .subtitle{font-size:15px}
    .message{font-size:14px}
    .cta-btn{padding:14px 28px;font-size:12px;letter-spacing:2px;width:100%;max-width:320px}
    .next-box{padding:12px 16px}
    .next-item{font-size:13px}
  }
</style>
</head>
<body>

<div class="page">

  <div id="starfield"></div>

  <!-- SELO -->
  <div class="selo">
    <img src="img/selo-coracao-cera2.png" alt="Selo de O Código do Amor Divino" onerror="this.style.display='none'">
  </div>

  <!-- HEADER -->
  <div class="eyebrow">Pagamento confirmado</div>
  <h1>Sua compra está confirmada.</h1>
  <p class="subtitle">Você acabou de dar o primeiro passo. O caminho começa agora.</p>

  <div class="divider"></div>

  <!-- MENSAGEM -->
  <div class="message">
    <p><strong>Chegou a hora de reconhecer.</strong> As feridas. Os sinais. Quem Deus separou pra você antes do mundo existir.</p>
  </div>

  <!-- BOTÃO -->
  <div class="cta-wrap">
    <a href="https://coracao-blindado.flowlink-app.online" class="cta-btn">Acessar seu produto agora →</a>
    <div class="cta-sub">Ou acesse direto pelo email que vai chegar</div>
  </div>

  <!-- SUPORTE -->
  <div class="support">
    <div class="support-label">Precisa de ajuda?</div>
    <div class="support-email">
      <a href="mailto:ainspiracaodivina@gmail.com">ainspiracaodivina@gmail.com</a>
    </div>
  </div>

  <!-- BÊNÇÃO -->
  <p class="blessing">Assim que você abrir, nunca mais será a mesma que abriu.</p>
  <div class="signature">por recados sagrados — 2026</div>

</div>

<script>
  (function(){
    const sf = document.getElementById('starfield');
    if(!sf) return;
    for(let i=0;i<30;i++){
      const s = document.createElement('div');
      s.className = 'star';
      s.style.left = Math.random()*100 + '%';
      s.style.top = Math.random()*100 + '%';
      s.style.opacity = (Math.random()*0.5 + 0.2).toFixed(2);
      s.style.width = (Math.random()*2 + 1).toFixed(1) + 'px';
      s.style.height = s.style.width;
      sf.appendChild(s);
    }
  })();
</script>

</body>
</html>
